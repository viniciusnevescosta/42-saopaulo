1)

Arquivos para entregar : ft_putchar.c
Funções ou bibliotecas autorizadas : write
• Escreva uma função que exiba o caractere passado como parâmetro.
• A função deve ser prototipada da seguinte forma:
void ft_putchar(char c);
• Para exibir o caractere, você deve usar a função write da seguinte forma:
write(1, &c, 1);